# ═══════════════════════════════════════════════════════
# FOOTBALL PREDICTOR — requirements.txt
# Install with: pip install -r requirements.txt
# Python version: 3.10+ recommended
# ═══════════════════════════════════════════════════════

# ── DATA ACQUISITION ──────────────────────────────────
requests==2.31.0              # HTTP requests for football-data.co.uk, ClubElo API
beautifulsoup4==4.12.3        # HTML parsing for Understat, FBref, Sofascore
lxml==5.1.0                   # Fast XML/HTML parser backend for BeautifulSoup
selenium==4.18.1              # JS-rendered pages (Sofascore live, when needed)
webdriver-manager==4.0.1      # Auto-downloads ChromeDriver for Selenium
cloudscraper==1.2.71          # Bypasses Cloudflare bot protection (Transfermarkt)

# ── DATA STORAGE & PROCESSING ─────────────────────────
pandas>=2.0.0,<3.0.0         # Core data manipulation — DataFrames, rolling windows
numpy>=1.24.0,<2.0.0         # Numerical arrays, Poisson/Dixon-Coles calculations
pyarrow>=15.0.0              # Read/write parquet files (fast columnar storage)
fastparquet>=2024.2.0        # Alternative parquet engine (fallback)

# ── FEATURE ENGINEERING ───────────────────────────────
scikit-learn>=1.6.0          # StandardScaler, TimeSeriesSplit, SMOTE-compatible pipeline
imbalanced-learn==0.12.0      # SMOTE for fixing Draw class imbalance
category-encoders==2.6.3      # Target encoding for team names (replaces one-hot)

# ── MACHINE LEARNING MODELS ───────────────────────────
xgboost>=2.1.0                # XGBoost gradient boosting (Market 1, 3, 4)
lightgbm>=4.3.0               # LightGBM (Market 1, 2 — faster on tabular data)
# sklearn includes LogisticRegression (Market 3) & RandomForest (Market 4) — see FEATURE ENGINEERING

# ── HYPERPARAMETER TUNING ─────────────────────────────
optuna==3.5.0                 # Bayesian hyperparameter optimisation (replaces GridSearch)
optuna-integration==3.5.0     # XGBoost and LightGBM callbacks for Optuna

# ── PROBABILISTIC MODELS ──────────────────────────────
scipy>=1.14.1                 # Poisson distribution, Dixon-Coles optimisation (minimize)
statsmodels>=0.14.3           # Logistic regression diagnostics, calibration utilities

# ── MODEL EXPLAINABILITY ──────────────────────────────
shap>=0.45.1                  # SHAP values — top 5 features per prediction
matplotlib>=3.9.0             # SHAP waterfall plots, confusion matrices

# ── EVALUATION METRICS ────────────────────────────────
# sklearn covers: accuracy, F1, ROC-AUC, Brier score, calibration_curve
# RPS (Ranked Probability Score) implemented manually in evaluate.py

# ── VISUALISATION ─────────────────────────────────────
matplotlib>=3.8.0             # Static plots (calibration curves, bankroll chart, SHAP plots)
seaborn>=0.13.0               # Heatmaps (corner tendency, correlation matrices)
plotly==5.20.0                # Interactive charts in notebooks and Streamlit dashboard

# ── PROGRESS BARS & LOGGING ───────────────────────────
tqdm==4.66.2                  # Progress bars on long scraping/training loops
loguru==0.7.2                 # Beautiful, structured logging (replaces print statements)

# ── JUPYTER NOTEBOOKS ─────────────────────────────────
jupyter==1.0.0                # Notebook server
ipykernel==6.29.3             # Python kernel for Jupyter
ipywidgets==8.1.2             # Interactive widgets inside notebooks (sliders, dropdowns)
nbformat==5.9.2               # Notebook file format utilities

# ── STREAMLIT DASHBOARD (Phase 2) ─────────────────────
streamlit>=1.41.0            # Dashboard framework
streamlit-option-menu==0.3.12 # Sidebar navigation menu component
plotly==5.20.0                # Interactive charts in Streamlit (already listed above)

# ── SCHEDULING & AUTOMATION ───────────────────────────
schedule==1.2.1               # Cron-style scheduling for live scraper (every 5 min)
python-dotenv==1.0.1          # Load environment variables from .env (API keys if needed)

# ── FILE & PATH UTILITIES ─────────────────────────────
pathlib2==2.3.7               # Pathlib backport (covered by stdlib in Python 3.10+)
joblib==1.3.2                 # Save/load sklearn models (.pkl files), parallel jobs

# ── DATE & TIME ───────────────────────────────────────
python-dateutil==2.9.0        # Parse date strings from multiple sources reliably

# ── TESTING (optional but recommended) ────────────────
pytest==8.1.0                 # Unit tests for each module
pytest-cov==4.1.0             # Coverage report

# ═══════════════════════════════════════════════════════
# INSTALL NOTES
# ═══════════════════════════════════════════════════════
# 1. Create a virtual environment first:
#       python -m venv venv
#       source venv/bin/activate          (Mac/Linux)
#       venv\Scripts\activate             (Windows)
#
# 2. Install all packages:
#       pip install -r requirements.txt
#
# 3. Verify key installs:
#       python -c "import xgboost, lightgbm, shap, optuna; print('All good')"
#
# 4. For Selenium (live scraping only):
#       pip install webdriver-manager
#       Chrome browser must be installed on your machine
# ═══════════════════════════════════════════════════════